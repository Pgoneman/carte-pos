/**
 * 설정 페이지 – 실제 설정 UI는 상단 ≡ 메뉴로 열리는 사이드바(ManagementDashboard)에서 제공.
 */
export default function SettingsPage() {
  return (
    <div className="p-6 text-gray-500 text-sm">
      설정 메뉴는 상단 왼쪽 ≡ 버튼을 눌러 열 수 있습니다.
    </div>
  );
}
